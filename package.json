{
  "name": "shiye-socks5",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-public-IP SOCKS5 and L2TP management panel",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/auth.js && node --check public/app.js && node --check public/login.js"
  },
  "engines": {
    "node": ">=18"
  }
}
